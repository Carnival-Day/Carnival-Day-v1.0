using UnityEngine;

public class SaveGameButton : MonoBehaviour
{
    /*
    public int saveFileIndex; // Set this in the Inspector
    public GameObject gameSavedText; // Assign the TextMeshPro GameObject in the Inspector

    public void SaveGameOnClick()
    {
        Debug.Log("Save Game button clicked.");

        DataPersistenceManager manager = DataPersistenceManager.Instance;
        if (manager != null)
        {
            manager.SaveGame(saveFileIndex);
            Debug.Log("Game saved to file index: " + saveFileIndex);

            // Activate the game saved text
            if (gameSavedText != null)
            {
                gameSavedText.SetActive(true);
                Debug.Log("Game Saved text activated.");
            }
        }
        else
        {
            Debug.LogError("DataPersistenceManager instance is null.");
        }
    }
    */

}
